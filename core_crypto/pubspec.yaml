name: core_crypto
description: AES-256-GCM encryption and Argon2id key derivation. No crypto logic lives outside this package.
version: 0.0.1
publish_to: none

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  cryptography: ^2.7.0

dev_dependencies:
  lints: ^3.0.0
  test: ^1.24.0
